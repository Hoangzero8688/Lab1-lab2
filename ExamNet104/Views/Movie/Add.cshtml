@model ExamNet104.ViewModel.MovieVM

@{
    ViewData["Title"] = "Add";
}

<h1>Add</h1>

<h4>MovieVM</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Add">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="name" class="control-label"></label>
                <input asp-for="name" class="form-control" />
                <span asp-validation-for="name" class="text-danger"></span>
            </div>

            <div class="form-group">
              
               @*  <font style="vertical-align: inherit;"><font style="vertical-align: inherit;">@Html.TextBox("name", "Pranaya", new { @class = "redtextbox form-control", @readonly="true" })</font></font>

                <font style="vertical-align: inherit;"><font style="vertical-align: inherit;">@Html.TextBox("name", "abc", new { style = "background-color:Red; color:White; font-weight:bold", title="Vui lòng nhập tên của bạn" })</font></font>
                @Html.Label("PassWord")
                @Html.Password("Password")
                @Html.Hidden("id")

                @Html.ActionLink("Home", "Index", "Home")<font></font>
                @Html.TextBoxFor(model => model.name) *@

                @Html.TextBox("Tên nhân viên", null, new { @class = "form-control", placeholder = "Nhập tên" })

               
                @Html.TextBoxFor(m => m.name, new { @class = "form-control m-5", placeholder = "Enter Name" })
            </div>
            <div class="form-group">
                <label asp-for="description" class="control-label"></label>
                <input asp-for="description" class="form-control" />
                <span asp-validation-for="description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="rating" class="control-label"></label>
                <input asp-for="rating" class="form-control" />
                <span asp-validation-for="rating" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
